name: codex
cli: codex
args:
  - "exec"
  - "--json"
  - "--dangerously-bypass-approvals-and-sandbox"
  - "-m"
  - "{{model}}"
  - "-c"
  - "model_reasoning_effort={{reasoning}}"
prompt_via: arg
continue_args:
  - "resume"
  - "{{session_id}}"
output_parser: codex
session_handler: codex

# Lead Session - 2026-01-17

## Mission

Complete all pending fixes autonomously, merge when ready.

## Tasks In Progress

### 1. fix/detected-merge-sort-order (PENDING)
- **Status**: Drafted, needs to send to worker
- **Problem**: Detected merges write `task.merged` events with `TS: now`, pushing old tasks to top of list
- **Fix**: In `history.Tail()`, skip events with `"via": "detected"` when computing `LastTS`
- **Files**: `task/history/history.go`
- **Complexity**: ~5 lines

### 2. fix/local-first-refs (PENDING)
- **Status**: Drafted, needs to send to worker
- **Problem**: Subtask prefers origin refs over local, violating local-first philosophy
- **Locations to fix**:
  - `cmd/subtask/draft.go:87-96` - fetches and prefers origin
  - `cmd/subtask/send.go:418-421` - uses origin for behind calc
  - `cmd/subtask/output.go:273-276` - uses origin for behind display
  - `task/index/gitcache.go:128-131` - uses origin for cache
  - `task/ops/close.go:129-132` - uses origin for cleanup check
  - `git/git.go:73-96` - SetupBranch auto-fetches, prefers origin
  - `git/git.go:431-457` - EffectiveTarget prefers origin
- **Fix**: Use local refs everywhere, remove auto-fetch, simplify EffectiveTarget
- **Note**: Keep integration.go's origin OBSERVATION (for detecting changes) - that's fine

### 3. fix/interrupted-display (NOT YET DRAFTED)
- **Status**: Needs to be drafted after #2 merges (overlapping files)
- **Problem**: After `subtask interrupt`, shows "error" instead of "interrupted"
- **Fix**:
  1. Ensure error message in send.go is just "interrupted" (currently "interrupted by SIGINT")
  2. In display logic, check if error == "interrupted" → show "interrupted"
- **Files**: `cmd/subtask/send.go`, `cmd/subtask/output.go`, `tui/view_list.go`

### 4. Behind stale after rebase (DEFERRED)
- **Status**: Needs design discussion, deferred
- **Problem**: After worker rebases, `subtask show` still shows "X behind with conflicts"
- **Question**: Should base_commit be updated after rebase? Or should display use merge-base?

---

## Completed This Session

### design/external-merge-detection (MERGED)
- Commit: fd3eac9
- Detects external merges via git ancestry + content checks
- Two-check approach: `merge-base --is-ancestor` + `merge-tree --write-tree`
- Closed tasks auto-promoted to merged when detected
- Revocable: clears if branch moves

---

## Execution Plan

1. Send #1 and #2 in parallel (no file overlap)
2. Review and merge #1 when ready
3. Review and merge #2 when ready
4. Draft and send #3 after #2 merges
5. Review and merge #3
6. Report completion to user

---

## Key Decisions Made

1. **Local-first philosophy**: Subtask uses local refs, never auto-fetches, user controls sync
2. **Detected merges**: Write events for durability but don't affect sort order
3. **Interrupted vs error**: Simple string check, ensure consistent message format
4. **Behind after rebase**: Deferred - needs design discussion

---

## Commands Reference

```bash
# Check task status
./subtask show <task>
./subtask list

# Send to worker
./subtask send <task> "message"

# Advance stage
./subtask stage <task> implement

# Merge when ready
./subtask merge <task> -m "message"

# Check worker output
./subtask log <task>
```
